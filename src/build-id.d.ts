// Injected by the pmdBuildVersion Vite plugin (see vite.config.ts) as a
// compile-time constant. Undefined under vitest (no define), so all reads go
// through `typeof __BUILD_ID__ === 'string'` guards.
declare const __BUILD_ID__: string;
